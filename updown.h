#ifndef _UPDOWN_H
#define _UPDOWN_H

enum { BYTES, KILOBYTES, MEGABYTES, GIGABYTES, HEXABYTE, PETABYTE };

class updown:
{
  updown(const & double uploaded, const & double downloaded, unsigned int metertype);
  ~updown();
  operator+(const & updown 
  
  
}
